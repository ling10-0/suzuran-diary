export function liveBroadcastTransform(){return{name:'suzuran-live-broadcast-transform',enforce:'pre',transform(code,id){if(!id.endsWith('/src/main.jsx'))return null;if(code.includes("import './liveBroadcastMount.jsx';"))return null;const anchor="import './newspaper.css';";if(!code.includes(anchor))return null;return{code:code.replace(anchor,anchor+"\nimport './liveBroadcastMount.jsx';"),map:null}}}}
